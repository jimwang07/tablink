{
  "name": "tablink-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typegen": "supabase gen types typescript --linked --schema public > packages/shared/src/supabase/types/database.types.ts"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  },
  "packageManager": "pnpm@9.15.0"
}
